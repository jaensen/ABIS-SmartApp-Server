mutation createSession {
    createSession {
        success
        errorMessage
        jwt
    }
}

mutation send($event:Json!) {
    send(event: $event) {
        success
        errorMessage
    }
}
