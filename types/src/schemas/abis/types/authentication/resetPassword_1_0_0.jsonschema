{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "abis-schema://abis@abis.internal/types/authentication/resetPassword_1_0_0",
  "title": "Reset password",
  "description": "Can be sent to the 'authenticationService' to reset a user's password. When the given 'email' address was correct, the user will be notified via email. This triggers a 'challenge' follow-up.",
  "type": "object",
  "properties": {
    "email": { "type": "string"}
  },
  "required": [
    "email"
  ]
}